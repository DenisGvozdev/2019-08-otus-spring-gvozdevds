docker pull mongo:4.0.4
docker run -d -p 27018-27019:27018-27019 --name mongodb mongo:4.0.4


cd C:/store/development/github/OTUS/2019-11-home_works/file-server
docker image build -t file-server:1.0.0 .
docker container run --publish 8000:8080 --name file-server file-server:1.0.0

docker container rm --force file-server



docker-compose up 
-----------------docker stack при включенном kubernates режим swarm----------
docker stack игнорирует сборку, поэтому указываем уже существующий image
  web:
    #build: .
    image: file-server:1.0.0


docker swarm init
docker stack deploy --orchestrator swarm --compose-file docker-compose.yml shopstack
docker stack services shopstack
docker container ls --all
docker stack rm shopstack
